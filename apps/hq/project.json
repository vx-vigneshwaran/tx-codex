{
  "name": "hq",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/hq/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/hq", "command": "pnpm dev" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/hq", "command": "pnpm build" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/hq", "command": "pnpm typecheck" }
    }
  }
}
