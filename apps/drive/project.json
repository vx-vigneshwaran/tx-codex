{
  "name": "drive",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/drive/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/drive", "command": "pnpm dev" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/drive", "command": "pnpm build" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/drive", "command": "pnpm typecheck" }
    }
  }
}
