{
  "name": "schoolos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/schoolos/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/schoolos", "command": "pnpm dev" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/schoolos", "command": "pnpm build" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/schoolos", "command": "pnpm typecheck" }
    }
  }
}
