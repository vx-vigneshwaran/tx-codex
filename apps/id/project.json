{
  "name": "id",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/id/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/id", "command": "pnpm dev" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/id", "command": "pnpm build" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/id", "command": "pnpm typecheck" }
    }
  }
}
